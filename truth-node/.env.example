# ==========================================
# TaaS Node Configuration
# ==========================================

# --- General System Settings ---
# Environment: 'production', 'development', or 'test'
NODE_ENV=production

# Logging Level: 'debug', 'info', 'warn', 'error'
LOG_LEVEL=info

# Node Mode: Determines the role of this node instance
# - 'sentinel':   Listens for requests and proposes outcomes (Earns fees)
# - 'challenger': Verifies other proposals and disputes errors (Secure the network)
# - 'both':       Runs both services concurrently
NODE_MODE=sentinel

# Unique identifier for this node (useful for logging in clustered setups)
NODE_ID=taas-node-01

# HTTP Port for API and Metrics
PORT=3001

# --- Blockchain Configuration (Helios Testnet) ---
# RPC Endpoint for the Helios Network
RPC_URL=https://testnet1.helioschainlabs.org

# Chain ID (42000 for Helios Testnet)
CHAIN_ID=42000

# --- Smart Contracts ---
# TruthOracleV2 Contract Address (The core protocol)
ORACLE_ADDRESS=0x383E24c68A57eCf2D728CaE2B93637c2fb608bE1

# TAASToken Contract Address (ERC20 used for staking)
TOKEN_ADDRESS=0x7e6ad72CFCC7395956a99C7441EF6A2EED1E376F

# --- Wallet Configuration ---
# Your EVM Private Key (Must start with 0x)
# WARNING: Keep this secret! Do not commit to version control.
PRIVATE_KEY=your_wallet_private_key_here

# --- Infrastructure & Service Endpoints ---
# Redis Connection (Required for job queues)
REDIS_HOST=localhost
REDIS_PORT=6379

# Truth Gateway / Indexer API (Required for Keyless Proxy Mode)
INDEXER_API_URL=http://localhost:3002

# --- Optional External API Keys (Legacy/Non-Proxy Mode) ---
# When INDEXER_API_URL is configured, the node operates in "Keyless Mode."
# These keys are only required if running in standalone mode without a Truth Gateway.

# Google Gemini (AI Analysis)
GEMINI_API_KEY=

# News & Finance
NEWS_API_KEY=
ALPHAVANTAGE_KEY=
COINGECKO_KEY=

# Sports & Weather
SPORTMONKS_KEY=
OPENWEATHER_API_KEY=
